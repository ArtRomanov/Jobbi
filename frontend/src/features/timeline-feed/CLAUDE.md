# Timeline Feed Feature

Alternative view to the kanban board showing chronological activity. Contains two components:

- **ActivityFeed** — paginated list of status change history entries (newest first)
- **ApplicationList** — table of all applications sorted by last updated

This is a `feature/` because it represents a user interaction (browsing and selecting applications by timeline), not just a domain entity.
