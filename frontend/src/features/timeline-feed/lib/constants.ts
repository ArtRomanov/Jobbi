/** Number of history entries to fetch per page in the activity feed. */
export const FEED_PAGE_SIZE = 50;

/** Number of applications to fetch for the application list view. */
export const LIST_PAGE_SIZE = 200;
